//! Audio abstraction placeholder. QBasic PLAY strings will be mapped later.

#[derive(Default)]
pub struct Audio;

impl Audio {
    pub fn new() -> Self {
        Self
    }

    pub fn play_intro(&self) {
        // No-op for the initial skeleton.
    }
}
